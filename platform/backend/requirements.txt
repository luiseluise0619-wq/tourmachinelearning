# --- 웹 프레임워크 ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic[email]>=2.6
python-multipart>=0.0.9      # 파일 업로드(CSV)

# --- DB ---
SQLAlchemy>=2.0
psycopg2-binary>=2.9         # PostgreSQL (SQLite는 기본 내장)

# --- 인증 (pbkdf2 표준 해시 사용) ---
PyJWT>=2.8

# --- AI / 데이터 (../src 재사용) ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
openpyxl>=3.1

# --- LLM / PDF ---
httpx>=0.27
reportlab>=4.0
python-dotenv>=1.0
