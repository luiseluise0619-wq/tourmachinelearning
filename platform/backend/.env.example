# DB (미설정 시 SQLite 자동: sqlite:///./festcast.db)
DATABASE_URL=

# 인증
JWT_SECRET=change-me-in-prod
JWT_EXPIRE_MIN=1440

# LLM (없으면 규칙 기반 폴백으로 동작)
LLM_PROVIDER=            # openai | gemini | (빈값=자동/폴백)
OPENAI_API_KEY=
GEMINI_API_KEY=

# CORS (프론트 주소)
CORS_ORIGINS=http://localhost:3000
